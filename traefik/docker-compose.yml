version: "3.4"

services:
  traefik:
    image: traefik:v2.2
    security_opt:
      - no-new-privileges:true
    container_name: traefik
    restart: unless-stopped
    # command:
      # --api.insecure=true --providers.docker
    networks:
      - web
      - internal
    ports:
      - "80:80"
      - "443:443"
    environment:
      - DO_AUTH_TOKEN=dop_v1_268214943d5e401fe2938500f81fc00df9195106249d28f85d8a27536d8e1975
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - TRAEFIK_PATH/traefik.yml:/traefik.yml:ro
      - TRAEFIK_PATH/acme.json:/acme.json
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik.entrypoints=http"
      - "traefik.http.routers.traefik.rule=Host(`traefik.ashbrookit.com`)"


      - "traefik.http.middlewares.traefik-https-redirect.redirectscheme.scheme=https"
      - "traefik.http.routers.traefik.middlewares=traefik-https-redirect"
      - "traefik.http.routers.traefik-secure.entrypoints=https"
      - "traefik.http.routers.traefik-secure.rule=Host(`traefik.ashbrookit.com`)"
      - "traefik.http.routers.traefik-secure.tls=true"
      - "traefik.http.routers.traefik-secure.tls.certresolver=le"
      - "traefik.http.routers.traefik-secure.tls.domains[0].main=ashbrookit.com"
      - "traefik.http.routers.traefik-secure.tls.domains[0].sans=*.ashbrookit.com"
      - "traefik.http.routers.traefik-secure.service=api@internal"

networks:
  web:
    external:
      name: web
  internal:
    external:
      name: internal
